In basic recursion, each call is growing recursively so it's O(n^2) efficiency.
We can also create a hast table to store and fetch previously calculated results which will increase
space needed but will drastically improve runtime efficiency.